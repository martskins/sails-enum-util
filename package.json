{
  "name": "sails-enum-util",
  "version": "1.0.5a",
  "description": "Utility to create Enums that behave like models living in memory",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "babel src --presets babel-preset-es2015 --out-dir dist",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/martskins/sails-enum-util.git"
  },
  "keywords": [
    "sails",
    "enum"
  ],
  "author": "Martin Asquino",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "sails-memory": "^0.10.7"
  }
}
